# deps & build output
node_modules/
dist/

# secrets — never commit the API key
.env

# personal working notes — not part of the deliverable
*.local.md

# committed data fixtures are the exception (grader runs fully offline):
# data/raw/ and data/enriched/ are deliberately tracked once they land.
